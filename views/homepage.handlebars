<div class="container mt-5">
  <h1 class="mb-3">Welcome to Your Posts!</h1>
  <div class="row">
    {{#each posts}}
      <div class="col-md-4">
        <div class="card mb-3">
          <div class="card-header bg-dark">
            <h5 class="card-title">
              <a href="/post/{{id}}" class="d-flex justify-content-center text-white">{{title}}</a>
            </h5>
          </div>
          <div class="d-flex justify-content-center card-body bg-body text-dark">
            <p>By: {{user.email}}</p>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>


{{!-- <!-- Loop over each object in the projects array -->
{{#each projects as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}
